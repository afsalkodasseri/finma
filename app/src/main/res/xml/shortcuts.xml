<?xml version="1.0" encoding="utf-8"?>
<shortcuts xmlns:tools="http://schemas.android.com/tools"
    xmlns:android="http://schemas.android.com/apk/res/android">
    <shortcut
        android:shortcutId="compose"
        android:enabled="true"
        android:icon="@drawable/ic_expense"
        android:shortcutShortLabel="@string/new_expense"
        android:shortcutLongLabel="@string/new_expense"
        android:shortcutDisabledMessage="@string/new_expense">
        <intent
            android:action="android.intent.action.VIEW"
            android:targetPackage="app.bicast.finma"
            android:targetClass="app.bicast.finma.NewExpenseActivity" />
        <!-- If your shortcut is associated with multiple intents, include them
             here. The last intent in the list determines what the user sees when
             they launch this shortcut. -->
        <categories android:name="android.shortcut.conversation" />
        <capability-binding android:key="actions.intent.CREATE_MESSAGE" />
    </shortcut>
</shortcuts>